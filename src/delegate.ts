import {
    DelegateEvent,
    DelegateCallback,
} from 'types';

export function add(elem: HTMLElement, types: string, selector: string, fn: DelegateCallback): void {

}

export function remove(elem: HTMLElement, types?: string, selector?: string, fn?: DelegateCallback): void {

}
